#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int num, s, e;
scanf("%d %d %d", &num, &s, &e); 
int d[num];
for(int i = 0; i < num; i++) {
    scanf("%d", &d[i]);
}
for(int i = 0; i < num; i++) {
    if(d[i] >= s && d[i] <= e) {
        printf("%d ", d[i]);
    }
}
}

