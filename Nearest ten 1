#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

        int n;
    scanf("%d", &n);

int remainder = n % 10;
if (remainder >= 5)
{
    printf("%d\n", n + (10 - remainder));
}
    else
    {
    printf("%d\n", n - remainder);
}
    return 0;
}
